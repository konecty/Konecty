# Example override file for local development customization
# Copy this file to docker-compose.override.yml and modify as needed
# This file is automatically loaded by docker-compose if it exists

name: konecty-dev

services:
  mongodb:
    # Uncomment to enable replica set for testing oplog features
    # command: mongod --bind_ip_all --replSet rs0
    # ports:
    #   - "27017:27017"
    #   - "27018:27018" # Additional port for replica set

  rabbitmq:
    # Uncomment to expose additional ports or change default ports
    # ports:
    #   - "5672:5672"
    #   - "15672:15672"

